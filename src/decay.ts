// exports.calcDecay = function calcDecay(date) {
//   if (simpul.isDate(date)) {
//     const diff = Math.abs(Date.now() - new Date(date));
//     const decay = Math.exp(-0.1 * (diff / (1000 * 60 * 60 * 24 * 30)));
//     return simpul.math.num(decay);
//   }
//   return 1;
// };
